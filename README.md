# Murph
Windows kernel-mode driver for comprehensive hardware identifier spoofing.  Randomizes CPU, GPU, disk, network, and SMBIOS firmware identifiers to prevent  system tracking and device fingerprinting. Pattern-based kernel module resolution  with exception handling and registry persistence.
